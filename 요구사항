1. 회원(Member) 엔티티와 공지사항(Notice) 엔티티는 일대다의 양방향 연관관계를 갖는다.
2. 값 타입(embdded)인 Address에는 city, street, zipCode를 받는다.
3. 공통 엔티티는 날짜와 작성자를 분리해서 각각 만든다.(회원 엔티티엔 날짜만, 공지사항 엔티티엔 날짜와 작성자가 모두 필요하기 때문)
4. 회원 리스트는 페이징이 필요 없고, 공지사항 리스트는 페이징 적용



//추가
1. 추후에 select절 최적화로 Projections 적용 한번 해볼 것.


